step_name: preprocessing-step # Name of the step in SageMaker Pipelines
script: preprocess.py # What script to run
description: Massages and preprocess the data # Step description
instance_type: ml.m5.xlarge # What instance type to use
num_instances: 1 # How many instances to use. Algorithm must support parallel processing if > 1.
